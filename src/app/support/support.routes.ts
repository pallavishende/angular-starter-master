import { Routes } from '@angular/router';

import { SupportlistComponent } from './support.component';

export const SupportRoutes: Routes = [
  { path: 'support', component: SupportlistComponent }
];
